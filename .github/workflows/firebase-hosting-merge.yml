# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
"on":
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    environment: "Test_Env"
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-java@v1
        with:
          java-version: "12.x"
      - uses: subosito/flutter-action@v1
        with:
          channel: "master"
      - name: "Decode env file"
        run: sudo echo "${ENV_SECRET// /}" | base64 --decode > .env
        env:
          API_KEY: ${{ secrets.API_KEY }}
      - name: "Decode base64 secrets"
        run: sudo echo "${API_KEY// /}" | base64 --decode > lib/env.dart
        env:
          API_KEY: ${{ secrets.API_KEY }}
      - name: "Run flutter pub get"
        run: flutter pub get
      - name: "View Secret File"
        run: sudo less lib/env.dart
      - name: "View Environment File"
        run: sudo less .env
      - name: "Generate Environment File"
        run: flutter pub run build_runner build
      - name: "Build Web App"
        run: flutter build web
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_FLUTTER_WEB_TEST_4B955 }}"
          channelId: live
          projectId: flutter-web-test-4b955
